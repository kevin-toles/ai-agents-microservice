# Core dependencies
fastapi>=0.109.0
uvicorn>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
langgraph>=0.0.40
langchain-core>=0.1.0
httpx>=0.26.0
structlog>=24.1.0
tenacity>=8.2.0
asyncio-throttle>=1.0.0
neo4j>=5.0.0

# MSEP dependencies (Multi-Stage Enrichment Pipeline)
numpy>=1.24.0

# Protocol Integration (WBS-PI5, WBS-PI6)
fastmcp>=0.1.0  # MCP server for exposing agent functions as tools
toolbox-core>=0.1.0  # MCP client for genai-toolbox (Neo4j, Redis)
aiofiles
